CC := gcc
SRC := 0-Makefile 1-Makefile 3-Makefile main.c README.md \
        2-Makefile 4-Makefile m.h school.c 100-Makefile
OBJ := $(SRC:.c=.o)
NAME := school
RM := rm -f
CFLAGS := -Wall -Werror -Wextra -pedantic
COMPILE = $(CC) $(OBJ) -o $@

.PHONY: all clean oclean fclean re

all: $(NAME)

$(NAME): $(OBJ)
	@$(COMPILE)

clean:
	-@$(RM) *~ $(NAME) clean

oclean:
	-@$(RM) $(OBJ) oclean

fclean: clean oclean
	-@$(RM) fclean

re: fclean all

# Ensure that the Makefile does not compile if m.h is missing
ifeq (,$(wildcard m.h))
$(error "m.h not found!")
endif

