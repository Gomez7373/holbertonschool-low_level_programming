#include<stdio.h>
/**
 * main - program prints from 0 to 10.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int s;

	for (s = 0; s < 10; s++)
{
	putchar("%d", s);
}
	putchar('\n');
	return (0);
}
