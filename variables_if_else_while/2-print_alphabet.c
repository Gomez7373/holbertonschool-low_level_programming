#include<stdio.h>
/**
 * main - Lower case prints.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s;

	for (s = 0; s < 26; s++)
{
	putchar('a' + s);
        
}
	putchar('\n');

	return (0);
}
