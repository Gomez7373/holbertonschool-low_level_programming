#include<stdio.h>
/**
 * main - Lower case prints.
 *
 * Return: Always 0 (Success)
 */
int main()
	char letter;

	for (letter = 'a' ; letter <= 'z'; letter++)
	putchar(letter);
{	
	putchar('\n');
}
	return 0;
}
	
