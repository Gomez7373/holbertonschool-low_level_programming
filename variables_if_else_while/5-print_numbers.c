#include <stdio.h>
/**
*
* main - prints all numbers of base 10 starting from 0, followed by a new line
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int s;

	for (s = 0; s < 10; s++)
{
	pritnf("%d", s);
}
	putchar('\n');
	return (0);
}
