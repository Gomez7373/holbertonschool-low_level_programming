#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
int result;

result = 1;

printf("Factorial of 0: %d\n", result);

result = 1;

printf("Factorial of 1: %d\n", result);

result = 2;

printf("Factorial of 2: %d\n", result);

return 0;
}

