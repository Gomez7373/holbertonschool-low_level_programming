#include "main.h"
#include <stdio.h>
#include <string.h>

void puts2(char *str)
{
	int g;
	
	g = 0;

    for (g = 0; str[g] && gg < 224; g += 2)
    {
        putchar(str[g]);

    }
   putchar('\n');

}
	
